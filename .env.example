# Optional: Set these environment variables to connect to an existing ZAP instance.
# If not set, DockerComposeUp will auto-generate an API key and use default values.

ZAP_API_KEY=change-me-to-your-secret-key
ZAP_BASE_URL=http://127.0.0.1:8090
